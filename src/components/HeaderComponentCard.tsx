
interface CardProps {
    logo: string;
    location: string;
    name: string;
}

export default function HeaderComponentCard({logo, name}: {logo: string, name: string}) {
    return (
            
    )
}