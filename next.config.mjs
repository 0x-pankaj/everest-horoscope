/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'reffect.co.jp', 'i0.wp.com', 'pastatic.picsart.com', 'cloud.appwrite.io', "people.com" ],
    }
};

export default nextConfig;  
